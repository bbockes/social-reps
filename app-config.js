// Placeholder values for offline use. CI overwrites this on deploy.
// For local cloud sync, copy config.example.js to config.js (gitignored, overrides this).
window.APP_CONFIG = {
  supabaseUrl: "https://YOUR_PROJECT.supabase.co",
  supabaseAnonKey: "YOUR_ANON_KEY"
};
